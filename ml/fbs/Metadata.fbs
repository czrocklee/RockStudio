include "Attributes.fbs";

namespace rs.ml.fbs;

table Metadata
{
  title:string (alias: "t");  
  artist:string (alias: "a");
  album:string (alias: "al");
  albumArtist:string (alias: "aa");
  genre:string (alias: "g");
  year:ushort (alias: "y");

  trackNumber:ushort (alias: "tn");
  totalTracks:ushort;
  discNumber:ushort (alias: "dn");
  totalDiscs:ushort;
}

root_type Metadata;
