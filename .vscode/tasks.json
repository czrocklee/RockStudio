{
    "tasks": [
        {
            "type": "shell",
            "label": "bazel build",
            "command": "bazel",
            "args": [
                "build",
                "--compilation_mode=dbg",
                "-s",
                "--spawn_strategy=local",
                "//..."
            ],

            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ],
    "version": "2.0.0"
}